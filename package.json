{
  "name": "dungeon-crawler",
  "version": "0.1.0",
  "description": "Multiplayer browser dungeon crawler with permadeath item loss",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test tests/**/*.test.js && node tools/content-validator.js && node tools/headless-sim.js --mainline",
    "test:unit": "node --test tests/**/*.test.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "pixi.js": "^7.4.3",
    "pngjs": "^7.0.0"
  }
}
