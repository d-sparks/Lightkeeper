All 6 content validation errors are resolved. Here's a summary:

**Content fixes (2):**
- `sol_components.json`: Renamed key `sol_cone_emitter` → `sol_cone` so the validator's key-based lookup matches
- `deep_perimeter_east.json` + `underlumen_approach.json`: Fixed flag name `seed_exposed_to_dark` → `umbral_seed_dark_exposed` to match what `homestead_interior.json` checks

**Validator fixes (3 false positives):**
- `warlord_defeated`: Was already set in `proc_quarantine.json` template triggers, but the validator only scanned static dungeons — now scans templates too
- `damage_booster_equipped` / `has_traded_meridian`: Already set in `server/index.js` engine code — added an engine-set flags allowlist to the validator
- `supply_manifest`: Already spawned in `proc_quarantine.json`'s `requiredRooms[].itemSpawns` — extended `scanObtainableItems` to check nested room spawns
