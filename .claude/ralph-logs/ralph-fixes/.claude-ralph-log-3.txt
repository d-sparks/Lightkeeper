Done. Here's a summary of what was changed:

**Medical supplies now use a charge counter on the medipac instead of taking inventory slots.**

### Server changes (6 files):

- **`server/game-loop.js`** — Player objects now have `medipacCharges: 0`. Picking up `medical_supplies` increments the counter (like silicon). `_useHeal` checks/decrements `medipacCharges` instead of searching inventory. Returns `'heal'` so index.js can send charge updates.

- **`server/index.js`** — All 9 INVENTORY messages now include `medipacCharges`. The ATTACK handler sends updated charges after a successful heal. Trade handler routes `medical_supplies` to charges.

- **`server/scripting/actions.js`** — `giveItem` and `removeItem` handle `medical_supplies` via the charge counter. All INVENTORY messages include `medipacCharges`.

- **`server/scripting/conditions.js`** — `hasItem: "medical_supplies"` checks the charge counter.

### Client changes (2 files):

- **`client/main.js`** — Stores `medipacCharges` from INVENTORY messages. Action bar shows a charge badge on the medipac heal slot (green number, turns gray at 0). Equipment menu shows "N supplies" on the medipac slot.

- **`client/index.html`** — CSS for `.charge-badge` (action bar) and `.medipac-charges` (equipment menu).
