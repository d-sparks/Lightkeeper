Done. Here's a summary of all changes:

**Content changes:**
- **`content/entities/items.json`** — Added `umbracite` item (uncommon key-type item, "A dense crystalline mineral found in the terminator zone")
- **`content/dungeons/train_station.json`** — Placed umbracite pickup at (12, 10) in the center hall. Changed the Meridian-7 trigger from giving free silicon to: check for umbracite in inventory → remove it → grant a solar panel structure → set `traded_umbracite_meridian` flag
- **`content/entities/npcs.json`** — Rewrote Meridian-7's dialogue with 4 states: `default` (asks for umbracite), `has_umbracite` (offers the trade), `traded` (confirms solar panel deployed), `returning` (existing). Also updated Tech Yara's mention from "silicon" to "umbracite"

**Engine changes (minimal, to support the new `giveStructure` action):**
- **`server/automation.js`** — Added `grantStructure(playerId, structureId)` method (builds without resource cost)
- **`server/scripting/actions.js`** — Added `giveStructure` action type that calls `automation.grantStructure` and sends updated AUTO_STATE to the client
