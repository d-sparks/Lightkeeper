Committed `TESTING.md` at `f422e7b`. Here's a summary of the plan:

**5 priority tiers**, ordered by value-per-effort:

1. **Standalone logic** — Flag store, event bus, automation. Zero mocks needed, highest ROI.
2. **Scripting with light mocks** — Conditions, actions, triggers, quest tracker. Mock flag store + player inventory.
3. **Physics** — Collision detection/resolution. Mock tile solidity.
4. **Integration** — Combat flow, equipment swaps, sol grid, room lifecycle. Use real subsystems with mocked content.
5. **Content validation** — Lint-style checks on all JSON: required fields, reference integrity, spawn bounds, quest DAG cycles.

**Excluded from testing**: client rendering (PixiJS), DOM/input handling, WebSocket transport, editor UI — all better tested manually.

**Framework**: Node's built-in test runner (`node --test`) to avoid adding dependencies.
